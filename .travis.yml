# Use new trusty images, should yield newer compilers and packages
sudo: required
dist: precise
language: cpp

matrix:
  include:
    - compiler: gcc
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
          packages:
            - g++-6
      env: COMPILER=g++-6
    - compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.8
          packages:
            - clang-3.8
      env: COMPILER=clang++-3.8
    - compiler: clang
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-precise-3.9
          packages:
            - clang-3.9
      env: COMPILER=clang++-3.9
    # - compiler: clang
    #   addons:
    #     apt:
    #       sources:
    #         - ubuntu-toolchain-r-test
    #         - llvm-toolchain-precise-4.0
    #       packages:
    #         - clang-4.0
    #   env: COMPILER=clang++-4.0

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libopencv-dev
install:
  - if [ "$CXX" = "g++-6" ]; then export CXX="g++-6" CC="gcc-6"; fi
  - if [ "$CXX" = "clang++-3.8" ]; then export CXX="clang++-3.8" CC="clang-3.9"; fi
  - if [ "$CXX" = "clang++-3.9" ]; then export CXX="clang++-3.9" CC="clang-3.9"; fi
script:
  - mkdir build
  - cd build
  - cmake -DCMAKE_CXX_COMPILER=$COMPILER .. && make
  - ./common_lib_tests
  - ./dual_number_lib_tests
  - ./interval_lib_tests
  - ./stream_lib_tests
  